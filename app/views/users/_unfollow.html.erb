<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
 <%= button_tag( :class => "ui bottom attached red button") do %>
    <i class="hart icon"></i>
　ありがとうございます！　　</br> 　 Now　</i><%= @user.followers.count %>　いいね！　
</div>
<% end %>
<% end %>
