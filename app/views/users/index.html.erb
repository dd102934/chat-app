<div class="ui massive celled list">
  <% @users.each do |user| %>
  <div class="item">
    <i class="big inverted green smile outline icon"></i>
    <div class="content">
      <div class="header">
        <%= link_to user.username, user_path(user) %>
    <% if logged_in? && current_user.admin? %>
      <%= link_to '削除', user, class: 'ui mini inverted red button', method: :delete,data: {confirm: "削除してもよろしいですか？"} %></td>
    <% end %>
      </div>
       <%= pluralize(user.messages.count, "message") %>
    </div>
    
  </div>
  <% end %>
</div>
<%= will_paginate @collection, renderer: WillPaginateSemanticUi::ActionView::Renderer %>

